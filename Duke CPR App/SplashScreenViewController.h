#import <UIKit/UIKit.h>
//Inherits from UIViewController instead of our own ViewControllers because we don't want behavior on the splash screen
@interface SplashScreenViewController : UIViewController

@end
